{
  "private": true,
  "scripts": {
    "test": "mocha --recursive",
    "test:watch": "mocha --recursive --watch --reporter progress"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "sinon": "^1.17.7"
  },
  "dependencies": {
    "express": "^4.14.0",
    "http-proxy-middleware": "^0.17.3"
  }
}
